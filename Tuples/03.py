# Finding Maximum, Minimum, and Sum in a Tuple

numbers = (10, 45, 32, 89, 21, 78)

# Finding max, min, and sum
max_value = max(numbers)
min_value = min(numbers)
sum_value = sum(numbers)

print("Maximum:", max_value)
print("Minimum:", min_value)
print("Sum:", sum_value)
